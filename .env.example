NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Hermes runs a separate headless execution runtime.
# Keep these server-only; never prefix them with NEXT_PUBLIC_.
HERMES_API_URL=
HERMES_API_KEY=
